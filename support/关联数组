在关联数组中,我们可以使用任何文本作为数组索引.而在普通数组中索引只能是从0开始的整数.

1.首先,需要特别申明一个变量为关联数组.这样才能使用:
declare -A array.

2.数组的赋值方式有两种
array={[index1]=value1 [index2]=value2}
array[index1]=value1

3.数组的调用
${!array[*]}		#列出数组的索引列表
${!array[@]}		#列出数组的索引列表
